/**
 * @file ProblemName.cpp
 * @brief LeetCode XXX. 题目中文名
 * @link https://leetcode.cn/problems/problem-name/
 *
 * 题目描述：
 * [在这里粘贴题目描述]
 *
 * 示例 1：
 * 输入：param1 = [示例输入]
 * 输出：[示例输出]
 * 解释：[解释说明]
 *
 * 示例 2：
 * 输入：param1 = [示例输入]
 * 输出：[示例输出]
 *
 * 提示：
 * - 约束条件 1
 * - 约束条件 2
 *
 * 解题思路：
 * [在这里写你的解题思路]
 * 1. 第一步：...
 * 2. 第二步：...
 * 3. 第三步：...
 *
 * 时间复杂度：O(?)
 * 空间复杂度：O(?)
 */

#include <vector>
#include <string>
// 根据需要添加其他头文件：
// #include <unordered_map>
// #include <unordered_set>
// #include <queue>
// #include <stack>
// #include <algorithm>

/**
 * @class ProblemName
 * @brief LeetCode XXX. 题目中文名 题解
 *
 * 功能：[描述这个类要解决的问题]
 * 职责：[描述这个类的职责]
 * 使用场景：LeetCode 第 XXX 题
 */
class ProblemName {
public:
    /**
     * @brief [方法功能简述]
     * @param param1 [参数1说明]
     * @param param2 [参数2说明]
     * @return [返回值说明]
     *
     * 功能：[详细描述方法功能]
     * 算法：[使用的算法名称]
     * 时间复杂度：O(?)
     * 空间复杂度：O(?)
     *
     * 示例：
     *   输入：param1 = [示例]
     *   输出：[示例]
     */
    ReturnType functionName(ParamType1 param1, ParamType2 param2) {
        // TODO: 实现你的解法

        // 1. 初始化变量

        // 2. 主要逻辑

        // 3. 返回结果
        return result;
    }

private:
    // 私有成员变量（如果需要）
    // int _memberVariable;  ///< 成员变量说明

    // 私有辅助方法（如果需要）
    /**
     * @brief 辅助方法说明
     * @param param 参数说明
     * @return 返回值说明
     */
    // ReturnType _helperFunction(ParamType param);

    // 常量定义（如果需要）
    // static const int kMaxSize = 1000;  ///< 最大大小
};

/**
 * @namespace TestProblemName
 * @brief 测试用例（可选）
 */
namespace TestProblemName {
    void test() {
        ProblemName solution;

        // 测试用例 1
        // auto result1 = solution.functionName(...);
        // assert(result1 == expected1);

        // 测试用例 2
        // auto result2 = solution.functionName(...);
        // assert(result2 == expected2);

        std::cout << "所有测试通过！" << std::endl;
    }
}

